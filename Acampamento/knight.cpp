#include <bits/stdc++.h>
#define all(x) x.begin(), x.end()
using namespace std;
typedef int num;
//REMEMBER TO DECLARE GLOBAL VARIABLES

int main(){
    std::ios::sync_with_stdio(false);
    queue<string> q;
    string a, b, t;
    cin >> a >> b;
    queue.push(a);
    while(queue.front()!=b){
        t = queue.pop();
    }
}
