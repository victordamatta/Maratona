
#include <bits/stdc++.h>
#define all(x) x.begin(), x.end()
#define ABC 1000000009
using namespace std;
typedef int num;
//REMEMBER TO DECLARE GLOBAL VARIABLES
/*
map<long long int, long long int> m;

long long int pd(long long int n){
    if(n==0) return 0;
    if(n==1) return 1;
    if(n==2) return 2;
    if(m.find(n) != m.end()) return m[n];
    long long int ans;
    ans = (pd(n-1)%ABC + pd(n-2)%ABC + pd(n-3)%ABC)%ABC;
    m[n] = ans;
    return ans;
}
*/
int main(){
    std::ios::sync_with_stdio(false);
    long long int n;
    while(cin>>n, n!=0){
        }
        cout <<  << "\n";
    }
    return 0;
}
